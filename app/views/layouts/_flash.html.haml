- if flash.keys.present?
  - if notice
    .alert-box.success.radius{"data-alert"=>""}
      = raw notice
      %a.close{:href => "#"}
  - if alert
    .alert-box.info.radius{"data-alert"=>""}
      = raw alert
      %a.close{:href => "#"}
